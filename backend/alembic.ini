# Alembic configuration
# Full config will be set up during migration phase.
# The sqlalchemy.url is overridden at runtime from DATABASE_URL.

[alembic]
script_location = alembic
sqlalchemy.url = %(DATABASE_URL)s
