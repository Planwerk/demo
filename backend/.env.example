# Required
JWT_SECRET=change-me-to-a-random-secret

# Database (default: local dev with placeholder credentials)
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/statusboard

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000

# JWT token lifetimes
# JWT_ACCESS_EXPIRE_MINUTES=30
# JWT_REFRESH_EXPIRE_DAYS=7

# WebSocket initial history window (hours)
# WS_INITIAL_HOURS=1

# Optional GitHub integration
# GITHUB_TOKEN=

# Logging
# LOG_LEVEL=INFO
